<section class="footer-links">
  <% if (pageData.previous) { %>
      <a class="previous" href="/<%= pageData.previous.id %>">
        <div class="footer-links__thumb">
          <!-- <img src="/images/thumb-bw-<%= pageData.previous.id %>.png" alt=""> -->

          <span class="footer-links__client"><%= pageData.previous.client %></span>

          <span class="footer-links__title"><%- pageData.previous.title %></span>

        </div>
        <div class="footer-links__chevron footer-links__chevron--left">
          <svg viewBox="0 0 300 300" preserveAspectRatio="xMidYMid meet">
            <use x="0" y="0" xlink:href="#chevron-left"></use>
          </svg>
        </div>
      </a>
    <% } %>
  <% if (pageData.next) { %>
      <a class="next" href="/<%= pageData.next.id %>">
        <div class="footer-links__thumb">
          <!-- <img src="/images/thumb-bw-<%= pageData.next.id %>.png" alt=""> -->
          <span class="footer-links__client"><%= pageData.next.client %></span>
          <span class="footer-links__title"><%- pageData.next.title %></span>

        </div>
        <div class="footer-links__chevron footer-links__chevron--right">
          <svg viewBox="0 0 300 300" preserveAspectRatio="xMidYMid meet">
            <use x="0" y="0" xlink:href="#chevron-right"></use>
          </svg>
        </div>
      </a>
  <% } %>
</section>
